import { Colors } from './colors';
import { Fonts, FontSizes } from './typography';
import { Spacing } from './spacing';

export const Theme = {
  colors: Colors,
  fonts: Fonts,
  fontSizes: FontSizes,
  spacing: Spacing,
};
